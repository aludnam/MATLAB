function n=update_n(w, a, b)
% Computes update for n_jk in variational approximation (Buntine & Jakulin
% DCA 2006)

[j,k]=size(w);
e_l = psi(a) - log(b); % (1 x K)
ntmp=w.*repmat(e_l,1,j);
z=sum(ntmp,1); % (1 x K)

n=ntmp./repmatz

