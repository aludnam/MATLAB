%kDate 'Date Object Menuform'
% This MatLab function was automatically generated by a converter (KhorosToMatLab) from the Khoros Date.pane file
%
% Parameters: 
% Integer: dateUpdatetime 'Update Time', default: 1: ' '
% String: dateFormat 'Format', default: '%h %d, 19%y %H:%M': 'date formatting string'
%
% Example: kDate( {'dateUpdatetime',1;'dateFormat','%h %d, 19%y %H:%M'})
%
% Khoros helpfile follows below:
% .begin tagged
% 
% .item "The Date Visual Object"
% A date visual object displays the current date according to the internal clock 
% of your computer. It is a specialized annotation subclassed from the string
% visual object.  The (x, y) location of the lower left corner of the date string 
% may be specified in device coordinates or world coordinates by the application. 
% The menuform, however, displays the world coordinate location of the date,
% regardless of whether the application used world or device coordinates to
% specify that location.  Attributes of the date object that can be
% interactively specified in addition to its world coordinate placement 
% include its format and the interval at which the date is updated, in addition
% to its justification, style, font, and colors.
% 
% .item "Justification"
% This attribute specifies the justification of the date "with respect to
% the width and height of the date object itself, not its parent".  Thus, 
% if the date object is just large enough to accomodate the date string, 
% justification will have little (if any) effect, as the date has no room to 
% "move" within its boundaries.  However, if the width and height are larger than 
% what is necessary to display the date, the justification will take effect.
% Justification may be set to "Center", "Top", "Bottom", "Left", "Right",
% "TopRight", "TopLeft", "BottomRight", or "BottomLeft".
% 
% .item "Fill Background"
% When set to true, this attribute indicates that the background of the date
% object should be filled with the fill color (background color).
% 
% .item "String Style"
% The date can be displayed in one of four styles: plain, emphasized, embossed
% in, or embossed out.  When the "plain" style is used, the date is drawn once,
% in the foreground color. When the date is "emphasized", it is double-drawn
% using both the foreground and highlight colors;  this causes it to have a bold,
% or emphasized, effect. When the "embossed in" style is used, the date will
% appear embossed "into" the background; both the foreground and highlight
% colors are used in embossing.  The "embossed out" style also causes the date
% to appear embossed, but the date will seem to come "out of" rather than
% "into" the background.
% 
% .item "Update Time"
% This integer value specifies how often, in seconds, that the date is updated.
% The default is to update the date every second.
% 
% .item "Format"
% The format in which the date is displayed.
% Formatting strings may use any of the following field 
% descriptors, which are taken directly from the "strftime" man page 
% (\f(CW% man strftime" for more information):
% 
% %%      same as %
% %a      day of week, using locale's abbreviated weekday names
% %A      day of week, using locale's full weekday names
% %b      month, using locale's abbreviated month names
% %B      month, using locale's full month names
% %c      date and time as %x %X
% %C      date and time, in locale's long-format representation
% %d      day of month (01-31)
% %D      date as %m/%d/%y
% %e      day of month (1-31; single digits are preceded by a blank)
% %H      hour (00-23)
% %I      hour (00-12)
% %j      day number of year (001-366)
% %k      hour (0-23; single digits are preceded by a blank)
% %l      hour (1-12; single digits are preceded by a blank)
% %m      month number (01-12)
% %M      minutes (0-59)
% %n      carriage return
% %p      locale's equivalent of AM or PM
% %r      time as %I:%M:%S %p
% %R      time as %H:%M
% %S      seconds (00-59)
% %t      tab
% %T      time as %H:%M:%S
% %U      week number of year (01-52), Sunday is the first day of the week
% %w      day of week; Sunday is day 0
% %W      week number of year (01-52), Monday is the first day of the week
% %x      date, using locale's date format
% %X      time, using locale's time format
% %y      year within century (00-99)
% %Y      year, including century (for example, 1988)
% %Z      time zone abbreviation
% 
% The difference between %U  and  %W  lies  in  which  day  is
% counted as the first day of the week.  Week number 01 is the
% first week with four or more January days in it.
% 
% .item "Font"
% Some of the fonts that are most commonly used with the date object are 
% available in the stringlist menu;  or, you may provide your own in the text 
% parameter box.
% .so  $DESIGN/manual/shared/xvobjects/general/FontName.sec
% 
% .item "Foreground Color"
% This stringlist selection lets you set the foreground color of the date object.
% The foreground color is always used to draw the string,
% regardless of the "String Style".
% 
% .item "Background Color"
% This stringlist selection lets you set the background color (fill color)
% of the date object.  It is only used when "Fill Background" is set to true.
% 
% .item "Highlight Color"
% This stringlist selection lets you set the highlight color; the highlight
% color is an additional color used in drawing the string when the "String Style"
% is set to "Emphasized", "Embossed In", or "Embossed Out".
% 
% .item "X Placement, Y Placement"
% This pair of double values specifies the (x, y) location of the lower left
% hand corner of the date visual object in world coordinates.
% 
% .end tagged


function varargout = kDate(varargin)
Inputs={};
if nargin ==0
  arglist={'',''};
elseif nargin ==1
  arglist=varargin{1};
else error('Usage: [out1,..] = kDate(arglist).');
end
if size(arglist,2)~=2
  error('arglist must be of form {''ParameterTag1'',value1;''ParameterTag2'',value2}')
 end
narglist={'dateUpdatetime', 1;'dateFormat', '%h %d, 19%y %H:%M'};
maxval={1,0};
minval={1,0};
istoggle=[0,0];
was_set=istoggle * 0;
paramtype={'Integer','String'};
% identify the input arrays and assign them to the arguments as stated by the user
if ~iscell(Inputs)
Inputs = {Inputs};
end
NumReqOutputs=0; nextinput=1; nextoutput=1;
  for ii=1:size(arglist,1)
  wasmatched=0;
  for jj=1:size(narglist,1)
   if strcmp(arglist{ii,1},narglist{jj,1})  % a given argument was matched to the possible arguments
     wasmatched = 1;
     was_set(jj) = 1;
     if strcmp(narglist{jj,2}, '__input')
      if (nextinput > length(Inputs)) 
        error(['Input ' narglist{jj,1} ' has no corresponding input!']); 
      end
      narglist{jj,2} = 'OK_in';
      nextinput = nextinput + 1;
     elseif strcmp(narglist{jj,2}, '__output')
      if (nextoutput > nargout) 
        error(['Output nr. ' narglist{jj,1} ' is not present in the assignment list of outputs !']); 
      end
      if (isempty(arglist{ii,2}))
        narglist{jj,2} = 'OK_out';
      else
        narglist{jj,2} = arglist{ii,2};
      end

      nextoutput = nextoutput + 1;
      if (minval{jj} == 0)  
         NumReqOutputs = NumReqOutputs - 1;
      end
     elseif isstr(arglist{ii,2})
      narglist{jj,2} = arglist{ii,2};
     else
        if strcmp(paramtype{jj}, 'Integer') & (round(arglist{ii,2}) ~= arglist{ii,2})
            error(['Argument ' arglist{ii,1} ' is of integer type but non-integer number ' arglist{ii,2} ' was supplied']);
        end
        if (minval{jj} ~= 0 | maxval{jj} ~= 0)
          if (minval{jj} == 1 & maxval{jj} == 1 & arglist{ii,2} < 0)
            error(['Argument ' arglist{ii,1} ' must be bigger or equal to zero!']);
          elseif (minval{jj} == -1 & maxval{jj} == -1 & arglist{ii,2} > 0)
            error(['Argument ' arglist{ii,1} ' must be smaller or equal to zero!']);
          elseif (minval{jj} == 2 & maxval{jj} == 2 & arglist{ii,2} <= 0)
            error(['Argument ' arglist{ii,1} ' must be bigger than zero!']);
          elseif (minval{jj} == -2 & maxval{jj} == -2 & arglist{ii,2} >= 0)
            error(['Argument ' arglist{ii,1} ' must be smaller than zero!']);
          elseif (minval{jj} ~= maxval{jj} & arglist{ii,2} < minval{jj})
            error(['Argument ' arglist{ii,1} ' must be bigger than ' num2str(minval{jj})]);
          elseif (minval{jj} ~= maxval{jj} & arglist{ii,2} > maxval{jj})
            error(['Argument ' arglist{ii,1} ' must be smaller than ' num2str(maxval{jj})]);
          end
        end
     end
     if ~strcmp(narglist{jj,2},'OK_out') &  ~strcmp(narglist{jj,2},'OK_in') 
       narglist{jj,2} = arglist{ii,2};
     end
   end
   end
   if (wasmatched == 0 & ~strcmp(arglist{ii,1},''))
        error(['Argument ' arglist{ii,1} ' is not a valid argument for this function']);
   end
end
% match the remaining inputs/outputs to the unused arguments and test for missing required inputs
 for jj=1:size(narglist,1)
     if  strcmp(paramtype{jj}, 'Toggle')
        if (narglist{jj,2} ==0)
          narglist{jj,1} = ''; 
        end;
        narglist{jj,2} = ''; 
     end;
     if  ~strcmp(narglist{jj,2},'__input') && ~strcmp(narglist{jj,2},'__output') && istoggle(jj) && ~ was_set(jj)
          narglist{jj,1} = ''; 
          narglist{jj,2} = ''; 
     end;
     if strcmp(narglist{jj,2}, '__input')
      if (minval{jj} == 0)  % meaning this input is required
        if (nextinput > size(Inputs)) 
           error(['Required input ' narglist{jj,1} ' has no corresponding input in the list!']); 
        else
          narglist{jj,2} = 'OK_in';
          nextinput = nextinput + 1;
        end
      else  % this is an optional input
        if (nextinput <= length(Inputs)) 
          narglist{jj,2} = 'OK_in';
          nextinput = nextinput + 1;
        else 
          narglist{jj,1} = '';
          narglist{jj,2} = '';
        end;
      end;
     else 
     if strcmp(narglist{jj,2}, '__output')
      if (minval{jj} == 0) % this is a required output
        if (nextoutput > nargout & nargout > 1) 
           error(['Required output ' narglist{jj,1} ' is not stated in the assignment list!']); 
        else
          narglist{jj,2} = 'OK_out';
          nextoutput = nextoutput + 1;
          NumReqOutputs = NumReqOutputs-1;
        end
      else % this is an optional output
        if (nargout - nextoutput >= NumReqOutputs) 
          narglist{jj,2} = 'OK_out';
          nextoutput = nextoutput + 1;
        else 
          narglist{jj,1} = '';
          narglist{jj,2} = '';
        end;
      end
     end
  end
end
if nargout
   varargout = cell(1,nargout);
else
  varargout = cell(0);
end
global KhorosRoot
if exist('KhorosRoot') && ~isempty(KhorosRoot)
w=['"' KhorosRoot];
else
if ispc
  w='"C:\Program Files\dip\khorosBin\';
else
[s,w] = system('which cantata');
w=['"' w(1:end-8)];
end
end
callKhoros([w 'Date" '],Inputs,narglist);
